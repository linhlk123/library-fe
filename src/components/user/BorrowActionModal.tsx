import React from 'react';\nimport { X, CheckCircle2, AlertCircle, Clock } from 'lucide-react';\n\nexport interface BorrowActionModalProps {\n  isOpen: boolean;\n  onClose: () => void;\n  action: 'BORROW' | 'HOLD';\n  bookTitle: string;\n}\n\nexport default function BorrowActionModal({\n  isOpen,\n  onClose,\n  action,\n  bookTitle,\n}: BorrowActionModalProps) {\n  if (!isOpen) return null;\n\n  const isBorrow = action === 'BORROW';\n  const headerColor = isBorrow ? 'from-blue-600 to-blue-700' : 'from-purple-600 to-purple-700';\n  const icon = isBorrow ? '📖' : '⏳';\n  const actionTitle = isBorrow ? 'Borrow Request' : 'Hold Request';\n  const details = isBorrow\n    ? {\n        step1: 'Request registered in the system',\n        step2: 'Pick up at the Library counter within 2 hours',\n        step3: 'Bring your reader card (valid for 6 months)',\n        step4: 'Borrow period: 4 days from pickup',\n      }\n    : {\n        step1: 'Hold request registered',\n        step2: 'We\\'ll reserve the book for you',\n        step3: 'Notification will be sent when available',\n        step4: 'Pick up within 7 days or hold expires',\n      };\n\n  return (\n    <div className=\"fixed inset-0 bg-black/60 backdrop-blur-sm flex items-center justify-center z-50 p-4\">\n      <div className=\"bg-gradient-to-br from-slate-900 via-slate-900 to-slate-950 border-2 border-slate-700 rounded-3xl w-full max-w-md shadow-2xl overflow-hidden animate-in fade-in zoom-in-95 duration-300\">\n        {/* Header */}\n        <div className={`bg-gradient-to-r ${headerColor} p-6 flex justify-between items-start`}>\n          <div>\n            <div className=\"text-5xl mb-2\">{icon}</div>\n            <h2 className=\"text-2xl font-bold text-white\">{actionTitle}</h2>\n          </div>\n          <button\n            onClick={onClose}\n            className=\"text-white/80 hover:text-white transition-colors p-1\"\n            aria-label=\"Close modal\"\n          >\n            <X size={24} />\n          </button>\n        </div>\n\n        {/* Content */}\n        <div className=\"p-6 space-y-6\">\n          {/* Book Title */}\n          <div>\n            <label className=\"text-xs font-semibold text-slate-400 uppercase tracking-wider\">Book Title</label>\n            <p className=\"text-lg font-bold text-cyan-300 mt-2 line-clamp-2\">{bookTitle}</p>\n          </div>\n\n          {/* Success Message */}\n          <div className=\"bg-emerald-500/20 border-2 border-emerald-500/50 rounded-2xl p-4 flex gap-3\">\n            <CheckCircle2 className=\"text-emerald-400 flex-shrink-0 mt-0.5\" size={20} />\n            <div>\n              <p className=\"text-sm font-semibold text-emerald-300 mb-1\">✅ Request Successfully Registered!</p>\n              <p className=\"text-xs text-emerald-200\">Your {isBorrow ? 'borrow' : 'hold'} request has been added to the system.</p>\n            </div>\n          </div>\n\n          {/* Steps */}\n          <div className=\"space-y-3\">\n            <h3 className=\"text-sm font-semibold text-white flex items-center gap-2\">\n              <Clock size={16} className=\"text-cyan-400\" />\n              Next Steps\n            </h3>\n            <ol className=\"space-y-2 text-sm\">\n              {Object.entries(details).map(([key, value], idx) => (\n                <li key={key} className=\"flex gap-3 text-slate-300\">\n                  <span className=\"flex-shrink-0 w-5 h-5 rounded-full bg-blue-600/30 border border-blue-500/50 flex items-center justify-center text-xs font-semibold text-blue-300\">\n                    {idx + 1}\n                  </span>\n                  <span>{value}</span>\n                </li>\n              ))}\n            </ol>\n          </div>\n\n          {/* Important Note */}\n          <div className=\"bg-amber-500/20 border-2 border-amber-500/50 rounded-2xl p-4 flex gap-3\">\n            <AlertCircle className=\"text-amber-400 flex-shrink-0 mt-0.5\" size={20} />\n            <div>\n              <p className=\"text-xs font-semibold text-amber-300 mb-1\">💳 Payment Method: Cash Only</p>\n              <p className=\"text-xs text-amber-200\">Late fees (1,000 VND/day) must be paid in cash at the Library counter.</p>\n            </div>\n          </div>\n\n          {/* Library Rules */}\n          <div className=\"bg-slate-800/80 border border-slate-700 rounded-2xl p-4\">\n            <p className=\"text-xs font-semibold text-slate-400 uppercase tracking-wider mb-2\">Library Rules</p>\n            <ul className=\"text-xs text-slate-300 space-y-1\">\n              <li>• <strong>Max Books:</strong> 5 per borrowing period</li>\n              <li>• <strong>Loan Period:</strong> 4 days from pickup</li>\n              <li>• <strong>Late Fee:</strong> 1,000 VND per day (cash only)</li>\n              <li>• <strong>Card Validity:</strong> 6 months from issuance</li>\n            </ul>\n          </div>\n        </div>\n\n        {/* Footer */}\n        <div className=\"px-6 py-4 bg-slate-800/50 border-t border-slate-700 flex gap-3\">\n          <button\n            onClick={onClose}\n            className=\"flex-1 px-4 py-3 rounded-xl bg-blue-600 hover:bg-blue-700 text-white font-semibold transition-all duration-300 active:scale-95 shadow-lg hover:shadow-blue-500/50\"\n          >\n            Understood\n          </button>\n          <button\n            onClick={onClose}\n            className=\"px-4 py-3 rounded-xl bg-slate-700 hover:bg-slate-600 text-white font-semibold transition-all duration-300 active:scale-95\"\n          >\n            Close\n          </button>\n        </div>\n      </div>\n    </div>\n  );\n}\n