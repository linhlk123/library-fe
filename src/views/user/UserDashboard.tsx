import React, { useMemo } from 'react';
import {
  Zap,
  TrendingUp,
  Clock,
  AlertTriangle,
  BookOpen,
  Calendar,
  CheckCircle2,
  AlertCircle,
} from 'lucide-react';

interface DashboardCard {
  title: string;
  value: string | number;
  subtitle?: string;
  icon: React.ReactNode;
  bgGradient: string;
  accentColor: string;
}

interface LibraryRules {
  maxBooks: number;
  loanDays: number;
  lateFine: number; // VND per day
  cardValidityMonths: number;
}

const LIBRARY_RULES: LibraryRules = {
  maxBooks: 5,
  loanDays: 4,
  lateFine: 1000, // VND
  cardValidityMonths: 6,
};

export interface DashboardData {
  activeBorrows: number;
  daysRemaining: number;
  totalFines: number;
  cardExpiresIn: number; // days
  newArrivals: Array<{
    id: string;
    title: string;
    subject: string;
    year: number;
    author: string;
  }>;
}

interface UserDashboardProps {
  data: DashboardData;
}

export default function UserDashboard({ data }: UserDashboardProps) {
  const stats = useMemo(() => {
    const overdueBooks = data.daysRemaining < 0 ? 1 : 0;
    const cardExpiryPercentage = (data.cardExpiresIn / (LIBRARY_RULES.cardValidityMonths * 30)) * 100;
    const borrowCapacity = LIBRARY_RULES.maxBooks - data.activeBorrows;
    const isOverdue = data.daysRemaining < 0;

    return {
      overdueBooks,
      cardExpiryPercentage: Math.max(0, cardExpiryPercentage),
      borrowCapacity,
      isOverdue,
      borrowPercentage: (data.activeBorrows / LIBRARY_RULES.maxBooks) * 100,
    };
  }, [data]);

  const yearCheckBadge = (year: number): boolean => {
    return new Date().getFullYear() - year <= 8;
  };

  return (\n    <div className=\"min-h-screen bg-gradient-to-br from-slate-950 via-slate-900 to-slate-950 p-4 sm:p-6 lg:p-8\">\n      {/* Premium Header Section */}\n      <div className=\"mb-12\">\n        <h1 className=\"text-4xl font-bold bg-gradient-to-r from-blue-400 via-cyan-400 to-blue-500 bg-clip-text text-transparent mb-2\">\n          Welcome Back 👋\n        </h1>\n        <p className=\"text-slate-400\">LibConnect LMS • IT-focused Library System</p>\n      </div>\n\n      {/* Bento Grid Layout */}\n      <div className=\"grid grid-cols-1 md:grid-cols-4 gap-4 lg:gap-6 auto-rows-max\">\n        {/* 1. Library Card Status - Square Card (2x2) */}\n        <div className=\"md:col-span-2 md:row-span-2\">\n          <div className=\"h-full bg-gradient-to-br from-slate-800 via-slate-800/50 to-transparent rounded-3xl border border-slate-700/50 backdrop-blur-sm p-6 sm:p-8 shadow-xl hover:shadow-2xl transition-shadow duration-300\">\n            <div className=\"flex items-start justify-between mb-6\">\n              <div>\n                <p className=\"text-sm font-semibold text-slate-400 uppercase tracking-wider\">Library Card Status</p>\n                <h3 className=\"text-2xl font-bold text-white mt-1\">6-Month Validity</h3>\n              </div>\n              <div className=\"p-3 rounded-2xl bg-gradient-to-br from-blue-500/20 to-cyan-500/20 border border-blue-500/30\">\n                <Calendar className=\"text-blue-400\" size={24} />\n              </div>\n            </div>\n\n            {/* Circular Progress */}\n            <div className=\"flex items-center justify-center relative mb-6\">\n              <svg className=\"w-40 h-40 transform -rotate-90\" viewBox=\"0 0 120 120\">\n                <circle\n                  cx=\"60\"\n                  cy=\"60\"\n                  r=\"54\"\n                  fill=\"none\"\n                  stroke=\"rgba(71, 85, 105, 0.3)\"\n                  strokeWidth=\"6\"\n                />\n                <circle\n                  cx=\"60\"\n                  cy=\"60\"\n                  r=\"54\"\n                  fill=\"none\"\n                  stroke=\"url(#grad1)\"\n                  strokeWidth=\"6\"\n                  strokeDasharray={`${(stats.cardExpiryPercentage / 100) * 339.29} 339.29`}\n                  strokeLinecap=\"round\"\n                  className=\"transition-all duration-500\"\n                />\n                <defs>\n                  <linearGradient id=\"grad1\" x1=\"0%\" y1=\"0%\" x2=\"100%\" y2=\"100%\">\n                    <stop offset=\"0%\" stopColor=\"#3b82f6\" />\n                    <stop offset=\"100%\" stopColor=\"#06b6d4\" />\n                  </linearGradient>\n                </defs>\n              </svg>\n              <div className=\"absolute flex flex-col items-center\">\n                <span className=\"text-3xl font-bold text-white\">{data.cardExpiresIn}</span>\n                <span className=\"text-xs text-slate-400 mt-1\">days left</span>\n              </div>\n            </div>\n\n            <div className=\"grid grid-cols-2 gap-3\">\n              <div className=\"bg-slate-700/30 rounded-xl p-3\">\n                <p className=\"text-xs text-slate-400 mb-1\">Card Type</p>\n                <p className=\"text-sm font-semibold text-white\">Active</p>\n              </div>\n              <div className=\"bg-slate-700/30 rounded-xl p-3\">\n                <p className=\"text-xs text-slate-400 mb-1\">Renewal</p>\n                <p className=\"text-sm font-semibold text-white\">{LIBRARY_RULES.cardValidityMonths}M</p>\n              </div>\n            </div>\n\n            {/* Rule Display */}\n            <div className=\"mt-6 pt-6 border-t border-slate-700/50\">\n              <p className=\"text-xs font-semibold text-slate-300 uppercase tracking-wider mb-3\">📋 Library Rules</p>\n              <div className=\"space-y-2 text-sm\">\n                <div className=\"flex items-center justify-between text-slate-300\">\n                  <span>Max Books</span>\n                  <span className=\"font-bold text-blue-400\">{LIBRARY_RULES.maxBooks}</span>\n                </div>\n                <div className=\"flex items-center justify-between text-slate-300\">\n                  <span>Loan Period</span>\n                  <span className=\"font-bold text-blue-400\">{LIBRARY_RULES.loanDays} days</span>\n                </div>\n                <div className=\"flex items-center justify-between text-slate-300\">\n                  <span>Late Fine</span>\n                  <span className=\"font-bold text-blue-400\">{LIBRARY_RULES.lateFine.toLocaleString()} VND/day</span>\n                </div>\n              </div>\n            </div>\n          </div>\n        </div>\n\n        {/* 2. Active Loans Status - Top Right */}\n        <div className=\"md:col-span-2\">\n          <div className=\"bg-gradient-to-br from-slate-800 via-slate-800/50 to-transparent rounded-3xl border border-slate-700/50 backdrop-blur-sm p-6 shadow-xl hover:shadow-2xl transition-shadow duration-300\">\n            <div className=\"flex items-start justify-between mb-4\">\n              <div>\n                <p className=\"text-sm font-semibold text-slate-400 uppercase tracking-wider\">Active Loans</p>\n                <h3 className=\"text-2xl font-bold text-white mt-1\">{data.activeBorrows}</h3>\n              </div>\n              <div className=\"p-3 rounded-2xl bg-gradient-to-br from-green-500/20 to-emerald-500/20 border border-green-500/30\">\n                <BookOpen className=\"text-green-400\" size={24} />\n              </div>\n            </div>\n\n            {/* Progress Bar */}\n            <div className=\"mt-6\">\n              <div className=\"flex justify-between items-end mb-2\">\n                <span className=\"text-xs text-slate-400\">Capacity</span>\n                <span className=\"text-sm font-semibold text-white\">{stats.borrowPercentage.toFixed(0)}%</span>\n              </div>\n              <div className=\"w-full h-3 bg-slate-700/50 rounded-full overflow-hidden\">\n                <div\n                  className=\"h-full bg-gradient-to-r from-green-500 to-emerald-400 rounded-full transition-all duration-500\"\n                  style={{ width: `${stats.borrowPercentage}%` }}\n                />\n              </div>\n              <p className=\"text-xs text-slate-400 mt-2\">\n                {stats.borrowCapacity} slot{stats.borrowCapacity !== 1 ? 's' : ''} available\n              </p>\n            </div>\n\n            {/* Days Remaining */}\n            <div className=\"mt-4 p-3 bg-slate-700/30 rounded-xl\">\n              <div className=\"flex items-center justify-between\">\n                <span className=\"text-sm text-slate-300\">Days Remaining</span>\n                <span className={`font-bold text-lg ${\n                  stats.isOverdue ? 'text-red-400' : data.daysRemaining <= 1 ? 'text-orange-400' : 'text-cyan-400'\n                }`}>\n                  {Math.abs(data.daysRemaining)} {stats.isOverdue ? '⚠️ OVERDUE' : 'days'}\n                </span>\n              </div>\n            </div>\n          </div>\n        </div>\n\n        {/* 3. Outstanding Fines - Square */}\n        <div>\n          <div className=\"bg-gradient-to-br from-slate-800 via-slate-800/50 to-transparent rounded-3xl border border-slate-700/50 backdrop-blur-sm p-6 shadow-xl hover:shadow-2xl transition-shadow duration-300 h-full\">\n            <div className=\"flex items-start justify-between mb-4\">\n              <div>\n                <p className=\"text-xs font-semibold text-slate-400 uppercase tracking-wider\">Outstanding</p>\n                <p className=\"text-xs text-slate-500\">Fines</p>\n              </div>\n              <div className={`p-3 rounded-2xl border ${\n                data.totalFines > 0\n                  ? 'bg-red-500/20 border-red-500/30'\n                  : 'bg-emerald-500/20 border-emerald-500/30'\n              }`}>\n                {data.totalFines > 0 ? (\n                  <AlertTriangle className=\"text-red-400\" size={20} />\n                ) : (\n                  <CheckCircle2 className=\"text-emerald-400\" size={20} />\n                )}\n              </div>\n            </div>\n            <div className={`text-3xl font-bold mt-auto ${\n              data.totalFines > 0 ? 'text-red-400' : 'text-emerald-400'\n            }`}>\n              {data.totalFines > 0 ? `${data.totalFines.toLocaleString()} VND` : '0 VND'}\n            </div>\n            <p className=\"text-xs text-slate-400 mt-3\">💳 Cash only</p>\n          </div>\n        </div>\n\n        {/* 4. New Arrivals - Horizontal Scroll (2x2) */}\n        <div className=\"md:col-span-2 md:row-span-2\">\n          <div className=\"bg-gradient-to-br from-slate-800 via-slate-800/50 to-transparent rounded-3xl border border-slate-700/50 backdrop-blur-sm p-6 sm:p-8 shadow-xl hover:shadow-2xl transition-shadow duration-300 h-full flex flex-col\">\n            <div className=\"mb-6\">\n              <p className=\"text-sm font-semibold text-slate-400 uppercase tracking-wider\">🆕 New IT Arrivals</p>\n              <h3 className=\"text-2xl font-bold text-white mt-1\">Latest Books</h3>\n            </div>\n\n            {/* Horizontal Scroll */}\n            <div className=\"flex-1 overflow-x-auto scrollbar-hide\">\n              <div className=\"flex gap-4 pb-4 min-w-max\">\n                {data.newArrivals.slice(0, 4).map((book) => (\n                  <div\n                    key={book.id}\n                    className=\"flex-shrink-0 w-48 bg-gradient-to-br from-slate-700/50 to-slate-800/50 rounded-xl border border-slate-700/50 p-4 hover:border-cyan-500/50 transition-all duration-300 group cursor-pointer\"\n                  >\n                    {/* Book Thumbnail */}\n                    <div className=\"w-full h-32 bg-gradient-to-br from-blue-500/20 to-cyan-500/20 rounded-lg mb-3 flex items-center justify-center border border-cyan-500/30 group-hover:border-cyan-500/60 transition-colors\">\n                      <BookOpen className=\"text-cyan-400\" size={32} />\n                    </div>\n\n                    {/* Book Info */}\n                    <h4 className=\"text-sm font-bold text-white line-clamp-2 mb-2 group-hover:text-cyan-400 transition-colors\">\n                      {book.title}\n                    </h4>\n\n                    {/* Subject Tag */}\n                    <div className=\"flex items-center gap-2 mb-3\">\n                      <span className=\"inline-block px-2.5 py-0.5 bg-gradient-to-r from-blue-500/30 to-cyan-500/30 border border-cyan-500/50 rounded-full text-xs font-semibold text-cyan-300\">\n                        {book.subject}\n                      </span>\n                    </div>\n\n                    {/* Year Check Badge */}\n                    <div className=\"flex items-center justify-between\">\n                      <span className=\"text-xs text-slate-400\">{book.author}</span>\n                      {yearCheckBadge(book.year) ? (\n                        <span className=\"inline-flex items-center gap-1 px-2 py-1 bg-emerald-500/20 rounded-lg border border-emerald-500/30\">\n                          <CheckCircle2 size={12} className=\"text-emerald-400\" />\n                          <span className=\"text-xs font-semibold text-emerald-300\">{book.year}</span>\n                        </span>\n                      ) : (\n                        <span className=\"inline-flex items-center gap-1 px-2 py-1 bg-orange-500/20 rounded-lg border border-orange-500/30\">\n                          <AlertCircle size={12} className=\"text-orange-400\" />\n                          <span className=\"text-xs font-semibold text-orange-300\">{book.year}</span>\n                        </span>\n                      )}\n                    </div>\n                  </div>\n                ))}\n              </div>\n            </div>\n\n            <p className=\"text-xs text-slate-400 mt-4 pt-4 border-t border-slate-700/50\">\n              ← Scroll to view more arrivals\n            </p>\n          </div>\n        </div>\n      </div>\n\n      {/* Disclaimer Banner */}\n      <div className=\"mt-8 p-4 bg-gradient-to-r from-blue-500/10 to-cyan-500/10 border border-blue-500/30 rounded-2xl backdrop-blur-sm\">\n        <div className=\"flex items-start gap-3\">\n          <Zap className=\"text-blue-400 flex-shrink-0 mt-1\" size={20} />\n          <div>\n            <p className=\"text-sm font-semibold text-blue-300 mb-1\">💡 LibConnect Rules (Quy định 8 điều)</p>\n            <p className=\"text-sm text-slate-300\">\n              Max <strong>{LIBRARY_RULES.maxBooks} books</strong> per <strong>{LIBRARY_RULES.loanDays} days</strong> • Late fine: <strong>{LIBRARY_RULES.lateFine.toLocaleString()} VND/day</strong> (Cash only) • Card valid for <strong>{LIBRARY_RULES.cardValidityMonths} months</strong>\n            </p>\n          </div>\n        </div>\n      </div>\n    </div>\n  );\n}\n